# Sessão 02: 05 de Março de 2026 - O Coração do Domínio

## 📌 O que foi feito hoje:

### 1. Versionamento e Conectividade
- Configuramos o **MCP do GitHub** para integrar ferramentas de gestão de repositório ao Gemini.
- Resolvemos divergências de histórico no Git utilizando `--allow-unrelated-histories` e conectamos o repositório local ao GitHub (`origin main`).

### 2. Ciclo TDD: Entidade de Usuário
- **Estado RED:** Criamos testes de unidade para a criação de usuários e validação de e-mails.
- **Estado GREEN:** Implementamos a entidade `User` em `src/modules/users/entities/user.py` usando `@dataclass`.
- **Estado REFACTOR:** Organizamos as validações dentro de `__post_init__` seguindo o **Clean Code** e o **Single Responsibility Principle (SRP)**.

### 3. Decisões Técnicas e Arquitetura
- **Validação de E-mail:** Analisamos o uso de Regex vs. `pydantic[email]`, optando pela robustez da biblioteca profissional para o futuro do projeto.
- **Segurança de Senha:** Definimos critérios de entropia e complexidade (12+ caracteres, mix de tipos) para o sistema financeiro.

---

## 🧠 O que aprendemos:
- **Single Responsibility Principle (SRP):** Como evitar o "God Method" no `__post_init__` através da delegação de validações.
- **Gerenciadores de Contexto (`with`):** O uso do `pytest.raises` para validar comportamentos negativos (exceções).
- **Entidades Autoválidas:** O conceito de que em Clean Architecture, uma entidade nunca deve existir em estado inválido.

---

## 🚀 Próximos Passos (To-Do):

1.  **Validações de Senha:** Finalizar os testes parametrizados para garantir senhas fortes.
2.  **Refactor Profissional:** Trocar a validação manual de e-mail pelo Pydantic.
3.  **Use Cases:** Iniciar a camada de aplicação com o caso de uso de criação de usuário.
4.  **Criptografia:** Implementar o hashing de senhas para que a entidade nunca armazene texto puro.

---
*Relatório gerado por: Gemini CLI (Mentor Técnico)*
